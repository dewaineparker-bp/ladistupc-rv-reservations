# RV Reservations Joomla update channel

The Joomla update feed is `updates/rvreservations.xml`.

Every advertised version must point to a complete installable component ZIP, never a patch-only ZIP. The update ZIP is published as `updates/com_rvreservations_<version>.zip` and should contain the full component manifest and all component files.
